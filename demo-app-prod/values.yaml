app:
  name: demo-app
  image:
    repository: argoproj/rollouts-demo
    tag: blue
  port: 8080
  replicas: 3

service:
  port: 80
  targetPort: 8080

virtualservice:
  host: demo-app.prod.local
  gateway: istio-system/my-gateway
